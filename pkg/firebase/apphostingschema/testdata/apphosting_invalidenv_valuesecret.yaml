schemaVersion: '3.0.0'

# Invalid as it contains both a value and secret field
env:
  - variable: STORAGE_BUCKET
    value: mybucket.appspot.com
    secret: myApiKeySecret
    availability:
      - BUILD
      - BACKEND