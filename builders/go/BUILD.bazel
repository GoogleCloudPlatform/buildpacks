load("//tools:defs.bzl", "builder")

package(default_visibility = ["//builders/go:__subpackages__"])

licenses(["notice"])

builder(
    name = "builder",
    buildpacks = [
        "//cmd/go:go.cnb",
    ],
    image = "gcp/go",
)
