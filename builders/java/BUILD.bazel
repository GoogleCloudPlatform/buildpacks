load("//tools:defs.bzl", "builder")

licenses(["notice"])

package(default_visibility = ["//builders/java:__subpackages__"])

builder(
    name = "builder",
    buildpacks = [
        "//buildpackages/java:buildpackage.cnb",
    ],
    image = "gcp/java",
)
