[project]
name = "pyproject_script"
version = "0.1.0"
description = ""
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.1",
]

[project.scripts]
start = "custom:app"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"