[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyproject_vendored_dependencies"
version = "0.0.1"
authors = []
description = ""
dependencies = [
    "sample-pip-dependency==0.0.1"
]