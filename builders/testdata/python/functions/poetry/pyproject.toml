[project]
name = "poetry"
version = "0.1.0"
description = ""
authors = []

[tool.poetry]
packages = [{include = "main.py"}]


[tool.poetry.dependencies]
functions-framework = [
  { version = "^3.0.0", python = ">= 3.8,<4"}
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
