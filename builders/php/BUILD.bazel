load("//tools:defs.bzl", "builder")

package(default_visibility = ["//builders/php/acceptance:__pkg__"])

licenses(["notice"])

builder(
    name = "builder",
    buildpacks = [
        "//cmd/php:php.cnb",
    ],
    image = "gcp/php",
)
