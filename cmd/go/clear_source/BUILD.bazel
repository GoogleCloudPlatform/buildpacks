load("@io_bazel_rules_go//go:def.bzl", "go_binary")
load("//tools:defs.bzl", "buildpack", "buildpack_using_runner")

licenses(["notice"])

buildpack(
    name = "clear_source",
    executables = [
        ":main",
    ],
    prefix = "go",
    version = "0.9.0",
    visibility = [
        "//builders:go_builders",
        "//builders:java_builders",
    ],
)

buildpack_using_runner(
    name = "clear_source_using_runner",
    buildpack_id = "google.go.clear-source",
    prefix = "go",
    version = "0.9.0",
    visibility = [
        "//builders:go_builders",
        "//builders:java_builders",
    ],
)

go_binary(
    name = "main",
    srcs = ["main.go"],
    # Strip debugging information to reduce binary size.
    gc_linkopts = [
        "-s",
        "-w",
    ],
    deps = [
        "//cmd/go/clear_source/lib",
        "//pkg/gcpbuildpack",
    ],
)
