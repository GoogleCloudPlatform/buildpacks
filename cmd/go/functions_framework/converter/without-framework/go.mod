module ffdeps

// The functions framework will be programmatically added as a dependency
// (`go mod edit -require`) so that it can be pinned to the version defined
// in the functions-framework buildpack's main.go.