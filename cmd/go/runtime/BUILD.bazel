load("@io_bazel_rules_go//go:def.bzl", "go_binary")

# Go runtime buildpack
load("//tools:defs.bzl", "buildpack", "buildpack_using_runner")

licenses(["notice"])

buildpack(
    name = "runtime",
    executables = [
        ":main",
    ],
    prefix = "go",
    version = "0.9.1",
    visibility = [
        "//builders:go_builders",
    ],
)

buildpack_using_runner(
    name = "runtime_using_runner",
    buildpack_id = "google.go.runtime",
    prefix = "go",
    version = "0.9.1",
    visibility = [
        "//builders:go_builders",
    ],
)

go_binary(
    name = "main",
    srcs = ["main.go"],
    # Strip debugging information to reduce binary size.
    gc_linkopts = [
        "-s",
        "-w",
    ],
    deps = [
        "//cmd/go/runtime/lib",
        "//pkg/gcpbuildpack",
    ],
)
