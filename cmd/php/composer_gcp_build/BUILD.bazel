load("@io_bazel_rules_go//go:def.bzl", "go_binary", "go_test")

# Buildpack for the PHP runtime.
load("//tools:defs.bzl", "buildpack", "buildpack_using_runner")

licenses(["notice"])

buildpack(
    name = "composer_gcp_build",
    executables = [
        ":main",
    ],
    prefix = "php",
    version = "0.9.1",
    visibility = [
        "//builders:php_builders",
    ],
)

buildpack_using_runner(
    name = "composer_gcp_build_using_runner",
    buildpack_id = "google.php.composer_gcp_build",
    prefix = "php",
    version = "0.9.1",
    visibility = [
        "//builders:php_builders",
    ],
)

go_binary(
    name = "main",
    srcs = ["main.go"],
    # Strip debugging information to reduce binary size.
    gc_linkopts = [
        "-s",
        "-w",
    ],
    deps = [
        "//pkg/gcpbuildpack",
        "//pkg/php",
    ],
)

go_test(
    name = "main_test",
    size = "small",
    srcs = ["main_test.go"],
    embed = [":main"],
    rundir = ".",
    deps = ["//internal/buildpacktest"],
)
