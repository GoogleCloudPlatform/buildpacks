load("@io_bazel_rules_go//go:def.bzl", "go_binary", "go_library", "go_test")

# Buildpack for the Python runtime.
load("//tools:defs.bzl", "buildpack", "buildpack_using_runner")

licenses(["notice"])

buildpack(
    name = "pip",
    executables = [
        ":main",
    ],
    prefix = "python",
    version = "0.9.2",
    visibility = [
        "//builders:python_builders",
    ],
)

buildpack_using_runner(
    name = "pip_using_runner",
    buildpack_id = "google.python.pip",
    prefix = "python",
    version = "0.9.2",
    visibility = [
        "//builders:python_builders",
    ],
)

go_binary(
    name = "main",
    srcs = ["main.go"],
    # Strip debugging information to reduce binary size.
    gc_linkopts = [
        "-s",
        "-w",
    ],
    deps = [
        "//pkg/buildermetrics",
        "//pkg/gcpbuildpack",
        "//pkg/python",
        "@com_github_buildpacks_libcnb_v2//:go_default_library",
    ],
)

go_library(
    name = "piplib",
    srcs = ["main.go"],  #keep
    importpath = "github.com/GoogleCloudPlatform/buildpacks/" + package_name(),
    visibility = [
        "//builders:python_builders",
    ],
    deps = [
        "//pkg/buildermetrics",
        "//pkg/gcpbuildpack",
        "//pkg/python",
        "@com_github_buildpacks_libcnb_v2//:go_default_library",
    ],
)

go_test(
    name = "main_test",
    size = "small",
    srcs = ["main_test.go"],
    embed = [":main"],
    rundir = ".",
    deps = ["//internal/buildpacktest"],
)
